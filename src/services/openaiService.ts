// This file is deprecated. Please use geminiService.ts instead.
// Keeping this file for backward compatibility.

import {
  generateChatResponse,
  extractDocumentInfo,
  type ChatMessage,
  type DocumentInfo,
} from "./geminiService";

// Re-export the functions and types from geminiService
export {
  generateChatResponse,
  extractDocumentInfo,
  type ChatMessage,
  type DocumentInfo,
};

// SYSTEM_PROMPT is now in geminiService.ts

// Interfaces are now imported from geminiService.ts

// generateChatResponse is now imported from geminiService.ts

// extractDocumentInfo is now imported from geminiService.ts
